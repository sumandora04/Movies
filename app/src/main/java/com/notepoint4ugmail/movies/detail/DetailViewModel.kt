package com.notepoint4ugmail.movies.detail

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import com.notepoint4ugmail.movies.network.MovieResponse

class DetailViewModel(movieResponse: MovieResponse,application: Application):AndroidViewModel(application) {
}